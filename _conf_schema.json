{
  "keyword": {
    "description": "触发关键字，直接发送该文字即可触发插件（留空则仅支持 /randompic 命令）",
    "type": "string",
    "default": "随机图片",
    "obvious_hint": true
  },
  "image_dir": {
    "description": "图片存放目录路径，支持相对路径（相对于AstrBot运行目录）和绝对路径",
    "type": "string",
    "default": "./random_pic",
    "obvious_hint": true
  },
  "count": {
    "description": "每次发送的图片数量",
    "type": "int",
    "default": 1,
    "obvious_hint": true
  },
  "recursive": {
    "description": "是否递归搜索子目录中的图片",
    "type": "bool",
    "default": true
  }
}
